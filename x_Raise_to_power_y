#include <stdio.h>
#include <math.h>
int main()
{
    int x,y,z;
    printf("Enter value of x:\n");
    scanf("%d",&x);
    printf("Enter value of y:\n");
    scanf("%d",&y);
    z=pow(x,y);
    printf("%d raise to power %d = %d ", x,y,z);
    return 0;
}
